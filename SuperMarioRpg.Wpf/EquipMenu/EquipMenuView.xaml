<Page x:Class="SuperMarioRpg.Wpf.EquipMenu.EquipMenuView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EquipMenuView">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <ContentPresenter
            Grid.Row="0" Grid.Column="0"
            Content="{Binding Path=LoadoutViewModel}"
            ContentTemplate="{StaticResource ResourceKey=LoadoutView}" />

        <ContentPresenter
            Grid.Row="1" Grid.Column="0"
            Content="{Binding Path=StatsViewModel}"
            ContentTemplate="{StaticResource ResourceKey=StatsView}" />
    </Grid>
</Page>
