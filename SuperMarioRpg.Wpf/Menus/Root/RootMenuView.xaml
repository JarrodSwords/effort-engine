<UserControl x:Class="SuperMarioRpg.Wpf.Menus.Root.RootMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:root="clr-namespace:SuperMarioRpg.Wpf.Menus.Root"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <ListView Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" ItemsSource="{Binding Path=PartyMembers}" />
        <ListView Grid.Row="0" Grid.Column="1" ItemsSource="{Binding Path=Submenus}" SelectedItem="{Binding Path=SelectedSubmenu}"/>
        <root:ResourcesView Grid.Row="1" Grid.Column="1" />
    </Grid>
</UserControl>
